<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techupstudio.countus" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".Helpers.CountusPreference"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Activities.SplashActivity"
            android:launchMode="singleTask"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.SignInActivity" android:launchMode="singleTask"/>
        <activity android:name=".Activities.SignUpActivity" android:launchMode="singleTask"/>
        <activity android:name=".Activities.ResetPasswordActivity" android:launchMode="singleTask"/>
        <activity android:name=".Activities.MainActivity" android:launchMode="singleTask"/>
        <activity android:name=".Activities.NewPersonActivity" android:launchMode="singleTask"/>
        <activity android:name=".Activities.AboutAppActivity" android:launchMode="singleTask"/>
        <activity android:name=".Activities.StatisticsActivity" android:launchMode="singleTask"/>
        <activity android:name=".Activities.SortPreferenceActivity" android:launchMode="singleTask"/>
        <activity android:name=".Activities.PersonDetailsActivity" android:launchMode="singleTask"/>
    </application>

</manifest>